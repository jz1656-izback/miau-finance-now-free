apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: miau-finance
  labels:
    app: miau-finance
    component: backend
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: miau-finance
    component: backend
